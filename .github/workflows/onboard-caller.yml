name: Kinen Onboarding

on:
  push:
    branches: [main]

permissions:
  contents: read
  issues: write

jobs:
  onboard:
    uses: kinen-club/workflows/.github/workflows/onboard.yml@main
    secrets: inherit
